version: '3'
services:
  mqttClient:
    build: .
    image: icebear8/mqttclient:build
    container_name: mqttclient
    volumes:
      - ./mqttApp:/mnt/app
    env_file:
      - ./config/default.env
